import { TestBed } from '@angular/core/testing';
import { PdCommentComponent } from './comment.component';

describe('PdCommentComponent', () => {
  it('should create the app', () => {
    const fixture = TestBed.createComponent(PdCommentComponent);
    const app = fixture.componentInstance;
    expect(app).toBeTruthy();
  });
});
